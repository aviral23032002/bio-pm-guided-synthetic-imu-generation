# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environments ───────────────────────────────────────────────────────
CS690r_project/
venv/
env/
.venv/
.env/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*

# ── Raw dataset (too large, ~50MB+) ───────────────────────────────────────────
WISDM_ar_v1.1/

# ── Preprocessed HDF5 files (large binary, re-generatable) ────────────────────
preprocessed_biopm/
preprocessed_biopm_test/
wisdm_windows.hdf5
*.h5
*.hdf5

# ── Large result files ─────────────────────────────────────────────────────────
results_week1/token_store.hdf5
results_week1_test/token_store.hdf5

# ── Feature npz files (large, re-generatable from scripts) ────────────────────
features/
*.npz

# ── Bio-PM model checkpoint (large, not ours) ─────────────────────────────────
CS690TR/checkpoints/checkpoint.pt

# ── Week 2: CAR-IMU checkpoints (large .pt files, re-trainable) ───────────────
car_imu_checkpoints/

# ── Week 2: Generated synthetic token stores (large HDF5, re-generatable) ─────
synthetic_tokens/
synthetic_tokens_*/

# ── Loss/result numpy files (small, keep tracked) ─────────────────────────────
# results_week2/*.npy  ← intentionally NOT ignored (small files, useful to share)

# ── Logs ──────────────────────────────────────────────────────────────────────
*.log
preprocess_all.log
generate.log

# ── IDE / editor ──────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Jupyter (checkpoints only — notebooks ARE tracked) ────────────────────────
.ipynb_checkpoints/

# ── Misc ──────────────────────────────────────────────────────────────────────
*.tmp
*.bak
